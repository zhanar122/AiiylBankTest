package aiylbank;

import aiylbank.controller.ProductController;
import aiylbank.model.Product;

    /*public class Main {
        public static void main(String[] args) {
            ProductController controller = new ProductController();

            // Тест 1: Успешное создание
            System.out.println(controller.createProduct(new Product(1, "Телефон", 500)));

            // Тест 2: Четный ID (ошибка)
            System.out.println(controller.createProduct(new Product(2, "Ноутбук", 1000)));

            // Тест 3: Простое число ID (ошибка)
            System.out.println(controller.createProduct(new Product(3, "Планшет", 300)));

            // Тест 4: Получение продукта
            System.out.println(controller.getProduct(1));

            // Тест 5: Удаление продукта
            System.out.println(controller.deleteProduct(1));
        }
    }
*/
